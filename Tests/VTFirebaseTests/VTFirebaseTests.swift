    import XCTest
    @testable import VTFirebase

    final class VTFirebaseTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(VTFirebase().text, "Hello, World!")
        }
    }
